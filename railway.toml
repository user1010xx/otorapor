[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python main.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Railway Variables (UI'dan ekleyin — secret'ları buraya yazmayın):
#   TELEGRAM_BOT_TOKEN
#   TELEGRAM_CHAT_ID
#   TONIVA_API_KEY
#   TZ=Europe/Istanbul
#   TONIVA_BASE_URL=https://crm.toniva.net/api/public/v1   (opsiyonel)
